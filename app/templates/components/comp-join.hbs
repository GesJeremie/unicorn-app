<h1>{{ model.server.name }}</h1>

{{input type="text" value=search enter="searchSong"}}

<br/>
{{#each tracks as |track|}}
    {{track.snippet.title}} <button {{action "pushNewSound"}}>Push sound</button><br/>
{{/each}}
